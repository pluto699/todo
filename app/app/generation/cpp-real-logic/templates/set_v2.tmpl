{% import 'generate.tmpl' as generate with context %}
{% if schema.package != None %}
    {% set namespace = schema.package | join('::') | replace(' ', '_') %}
{% else %}
    {% set namespace = 'sbe' %}
{% endif %}
{% set c_name = type.name | fmt_class_name %}
{% set cpp_t = type.encoding_type.name | replace_keyword %}

// Generated Simple Binary Encoding message codec
// https://github.com/ksergey/sbe-code-gen

#pragma once

#if defined(SBE_HAVE_CMATH)
/* cmath needed for std::numeric_limits<double>::quiet_NaN() */
#  include <cmath>
#  define SBE_FLOAT_NAN std::numeric_limits<float>::quiet_NaN()
#  define SBE_DOUBLE_NAN std::numeric_limits<double>::quiet_NaN()
#else
/* math.h needed for NAN */
#  include <math.h>
#  define SBE_FLOAT_NAN NAN
#  define SBE_DOUBLE_NAN NAN
#endif

#if __cplusplus >= 201103L
#  define SBE_CONSTEXPR constexpr
#  define SBE_NOEXCEPT noexcept
#else
#  define SBE_CONSTEXPR
#  define SBE_NOEXCEPT
#endif

#if __cplusplus >= 201703L
#  include <string_view>
#  define SBE_NODISCARD [[nodiscard]]
#else
#  define SBE_NODISCARD
#endif

#if !defined(__STDC_LIMIT_MACROS)
#  define __STDC_LIMIT_MACROS 1
#endif

#include <cstdint>
#include <cstring>
#include <iomanip>
#include <limits>
#include <ostream>
#include <stdexcept>
#include <sstream>
#include <string>
#include <vector>
#include <tuple>

#if defined(WIN32) || defined(_WIN32)
#  define SBE_BIG_ENDIAN_ENCODE_16(v) _byteswap_ushort(v)
#  define SBE_BIG_ENDIAN_ENCODE_32(v) _byteswap_ulong(v)
#  define SBE_BIG_ENDIAN_ENCODE_64(v) _byteswap_uint64(v)
#  define SBE_LITTLE_ENDIAN_ENCODE_16(v) (v)
#  define SBE_LITTLE_ENDIAN_ENCODE_32(v) (v)
#  define SBE_LITTLE_ENDIAN_ENCODE_64(v) (v)
#elif __BYTE_ORDER__ == __ORDER_LITTLE_ENDIAN__
#  define SBE_BIG_ENDIAN_ENCODE_16(v) __builtin_bswap16(v)
#  define SBE_BIG_ENDIAN_ENCODE_32(v) __builtin_bswap32(v)
#  define SBE_BIG_ENDIAN_ENCODE_64(v) __builtin_bswap64(v)
#  define SBE_LITTLE_ENDIAN_ENCODE_16(v) (v)
#  define SBE_LITTLE_ENDIAN_ENCODE_32(v) (v)
#  define SBE_LITTLE_ENDIAN_ENCODE_64(v) (v)
#elif __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
#  define SBE_LITTLE_ENDIAN_ENCODE_16(v) __builtin_bswap16(v)
#  define SBE_LITTLE_ENDIAN_ENCODE_32(v) __builtin_bswap32(v)
#  define SBE_LITTLE_ENDIAN_ENCODE_64(v) __builtin_bswap64(v)
#  define SBE_BIG_ENDIAN_ENCODE_16(v) (v)
#  define SBE_BIG_ENDIAN_ENCODE_32(v) (v)
#  define SBE_BIG_ENDIAN_ENCODE_64(v) (v)
#else
#  error "Byte Ordering of platform not determined. Set __BYTE_ORDER__ manually before including this file."
#endif

#if defined(SBE_NO_BOUNDS_CHECK)
#  define SBE_BOUNDS_CHECK_EXPECT(exp, c) (false)
#elif defined(_MSC_VER)
#  define SBE_BOUNDS_CHECK_EXPECT(exp, c) (exp)
#else
#  define SBE_BOUNDS_CHECK_EXPECT(exp, c) (__builtin_expect(exp, c))
#endif

#define SBE_NULLVALUE_INT8 (std::numeric_limits<std::int8_t>::min)()
#define SBE_NULLVALUE_INT16 (std::numeric_limits<std::int16_t>::min)()
#define SBE_NULLVALUE_INT32 (std::numeric_limits<std::int32_t>::min)()
#define SBE_NULLVALUE_INT64 (std::numeric_limits<std::int64_t>::min)()
#define SBE_NULLVALUE_UINT8 (std::numeric_limits<std::uint8_t>::max)()
#define SBE_NULLVALUE_UINT16 (std::numeric_limits<std::uint16_t>::max)()
#define SBE_NULLVALUE_UINT32 (std::numeric_limits<std::uint32_t>::max)()
#define SBE_NULLVALUE_UINT64 (std::numeric_limits<std::uint64_t>::max)()

{% for str in schema.package %}
namespace {{ str | replace(' ', '_') }} {
{% endfor %}

class {{ c_name }}
{
{# generateFixedFlyweightCode(...) #}
{{ generate.fixedFlyweightCode(c_name, type) }}

    {{ c_name }} &clear()
    {
        {{ cpp_t }} zero = 0;
        std::memcpy(m_buffer + m_offset, &zero, sizeof({{ cpp_t }}));
        return *this;
    }

    SBE_NODISCARD bool isEmpty() const
    {
        {{ cpp_t }} val;
        std::memcpy(&val, m_buffer + m_offset, sizeof({{ cpp_t }}));
        return 0 == val;
    }

    SBE_NODISCARD {{ cpp_t }} rawValue() const
    {
        {{ cpp_t }} val;
        std::memcpy(&val, m_buffer + m_offset, sizeof({{ cpp_t }}));
        return val;
    }

    {{ c_name }} &rawValue({{ cpp_t }} value)
    {
        std::memcpy(m_buffer + m_offset, &value, sizeof({{ cpp_t }}));
        return *this;
    }

{# generateChoices(...) #}
{% set byte_order_str = generate.formatByteOrderEncoding(type.encoding_type) %}
{% for choice in type.choices %}
    static bool {{ choice.name | lower_first }}(const {{ cpp_t }} bits)
    {
        return (bits & (static_cast<{{ cpp_t }}>(1) << {{ choice.value }}u)) != 0;
    }

    static {{ cpp_t }} {{ choice.name | lower_first }}(const {{ cpp_t }} bits, const bool value)
    {
        return value ? static_cast<{{ cpp_t }}>(bits | (static_cast<{{ cpp_t }}>(1) << {{ choice.value }}u)) : static_cast<{{ cpp_t }}>(bits & ~(static_cast<{{ cpp_t }}>(1) << {{ choice.value }}u));
    }

    SBE_NODISCARD bool {{ choice.name | lower_first }}() const
    {
        {{ cpp_t }} val;
        std::memcpy(&val, m_buffer + m_offset, sizeof({{ cpp_t }}));
        return ({{ byte_order_str }}(val) & (static_cast<{{ cpp_t }}>(1) << {{ choice.value }}u)) != 0;
    }

    {{ c_name }} &{{ choice.name | lower_first }}(const bool value)
    {
        {{ cpp_t }} bits;
        std::memcpy(&bits, m_buffer + m_offset, sizeof({{ cpp_t }}));
        bits = {{ byte_order_str }}(value ? static_cast<{{ cpp_t }}>({{ byte_order_str }}(bits) | (static_cast<{{ cpp_t }}>(1) << {{ choice.value }}u)) : static_cast<{{ cpp_t }}>({{ byte_order_str }}(bits) & ~(static_cast<{{ cpp_t }}>(1) << {{ choice.value }}u)));
        std::memcpy(m_buffer + m_offset, &bits, sizeof({{ cpp_t }}));
        return *this;
    }

{% endfor %}
{# generateChoicesDisplay(...) #}
    template<typename CharT, typename Traits>
    friend std::basic_ostream<CharT, Traits> & operator << (
        std::basic_ostream<CharT, Traits> &builder, {{ c_name }} writer)
    {
        builder << '[';
{% if type.choices | length > 1 %}
        bool atLeastOne = false;
{% endif %}
{% for choice in type.choices %}
        if (writer.{{ choice.name | lower_first }}())
        {
    {% if not loop.first %}
            if (atLeastOne)
            {
                builder << ",";
            }
    {% endif %}
            builder << R"("{{ choice.name | lower_first }}")";
    {% if not loop.last %}
            atLeastOne = true;
    {% endif %}
        }
{% endfor %}
        builder << ']';
        return builder;
    }
};

{% for str in schema.package %}
}
{% endfor %}
